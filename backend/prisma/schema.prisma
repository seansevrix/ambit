// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

// datasource db {
//   provider = "sqlite"
//   url      = "file:./dev.db"
// }


model Opportunity {
  id            Int    @id @default(autoincrement())
  noticeId      String
  title         String
  agency        String
  naics         String
  setAside      String
  status        String
  dueDate       String
  valueEstimate Int
  samLink       String
  notes         String
}
